Boolean = tags.
    case(true) 
    case(false) 

(this Boolean).or(that Boolean) = 
    this.
        case(true):
            (Boolean.true)
        case(false):
            (that)

(this Boolean).and(that Boolean) = 
    this.
        case(true):
            (that)
        case(false):
            (Boolean.false)

(this Boolean).not = 
    this.
        case(true):
            (Boolean.false)
        case(false):
            (Boolean.true)